# Use the current Calcit setup action

- Upgrade `calcit-lang/setup-calcit` from v1.3.0 to v1.4.0.
- Let CI install the Calcit 0.13.76 compiler baseline and the independently
  versioned `caps` tool without treating both tools as one release asset set.
