# Calcit 0.14.11 / Calcit 0.14.11

## 中文

- 将 Calcit 从 0.14.7 升级到 0.14.11，保持异步 fetch 的 C-safe 契约与模块版本不变。
- `--check-only`、`--warn-dyn-method`、dynamic-method 门禁与带 baseline 的质量门禁通过；依赖 dylib 的示例/FFI 门禁由 CI 构建后执行。
- 固定 CI 的 GitHub Actions 到不可变 commit。

## English

- Upgrade Calcit from 0.14.7 to 0.14.11, preserving the async-fetch C-safe contract and module version.
- `--check-only`, `--warn-dyn-method`, the dynamic-method gate, and the baseline quality gate pass; dylib-dependent example/FFI gates run in CI after `cargo build`.
- Pin the CI GitHub Actions to immutable commits.
