# Calcit 0.15.3 语法检查 / Calcit 0.15.3 syntax checks

## 中文

- 将模块与文档固定到 Calcit 0.15.3。
- 移除未使用且已归档的 `calcit-test` 开发依赖；测试已使用项目自身的 typed entry。
- 使用 `surface-latest-v2` 检查完整 Snapshot。

## English

- Pin the module and documentation to Calcit 0.15.3.
- Remove the unused, archived `calcit-test` development dependency; tests already use the project's typed entry.
- Check the full Snapshot with `surface-latest-v2`.

## 验证 / Verification

- `calcit fix --preset surface-latest-v2 --format edn`
- strict Calcit checks and quality analysis
- Rust format, test, clippy, and release build
- C-safe async export and cancellation regression checks
