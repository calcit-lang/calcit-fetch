# 发布 0.0.20 / Release 0.0.20

- 升级并验证 Calcit 0.13.59。
- 保持 fetch 的 `Unit` 启动契约：native 协议没有 cancel hook，因此不伪造 `FfiTask`。
- 修正文档示例，使 native 边界示例可由文档检查稳定验证。

- Upgraded and validated Calcit 0.13.59.
- Preserved fetch's `Unit` start contract: the native protocol has no cancel hook, so it does not fabricate an `FfiTask`.
- Corrected documentation examples so native-boundary snippets pass stable docs validation.
