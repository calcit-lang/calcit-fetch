# Release calcit-fetch 0.0.24

- Publish the Calcit 0.13.70 and calcit-test 0.0.7 snapshot requirements.
- Publish the native async Interface IR lowering contract.
- Keep options and callback types behind the documented handwritten adapter.
- Include the CI guard for versioned FFI metadata.
